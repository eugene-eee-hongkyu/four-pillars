# 05 B-1 로컬에서만들기 v2

> 00 기획 하네스 정책 v11을 토대로 한다. 작성 전 정책 문서의 "안 읽힘 6원인"·"문장 원칙"·"AI의 역할"·"번들링"·"변경 가시화"·"턴 카운팅"을 참조한다.

---

## 이 문서 사용법

B-1은 **하네스의 네 번째 핵심 문서**이다. A-2에서 Go 판정을 받은 화면 스펙을 **로컬 머신에서 Claude Code가 `--dangerously-skip-permissions` 모드로 헤매지 않고 실행할 수 있는 한 장짜리 빌드 지침서**로 변환한다.

**B-1의 정체성: 로컬 검증.**
- 인프라(Vercel·Supabase 등 SaaS) 배제. 로컬 머신에서만 실행.
- 시나리오 1·2 PASS가 1차 목표.
- 인프라로 올릴 때 구조 변경이 최소화되도록 **포터빌리티 체크리스트** 13개 자동 검증.

**B-2와의 분리:**
- B-1: 로컬 실행 + 시나리오 PASS + 포터빌리티 검증
- B-2: B-1 결과물을 인프라로 배포 (별개 산출물)

이 문서에는 두 가지가 같이 들어 있다:
1. **산출물 템플릿** — B-1 산출물이 최종적으로 어떤 형태여야 하는가
2. **워크플로우** — Claude와 플래너가 어떤 순서로 채워가는가

Claude는 이 문서와 기획 하네스 정책 v11을 함께 읽은 뒤 B-1 산출물 작성을 시작한다.

A-0/A-1/A-2/A-3 산출물이 B-1의 입력이다. 특히:
- **A-2** (화면 목록·Wireflow·화면별 4-a/b/c/d 상세 스펙) — 구조 입력
- **A-3** (디자인 토큰·Claude Design handoff bundle) — 시각 입력

A-3 없이 B-1 진입하면 디자인이 빌드 도구 디폴트로 채워져 타겟에 안 맞는 결과 나옴. A-3 우회는 명시적 결정일 때만 (예: 사내 도구라 디자인 무관).

B-1 산출물이 Go로 끝나면 Claude Code `--dangerously-skip-permissions` 세션으로 넘어간다. B-1 산출물이 그 세션의 **유일한 실행 지침서**가 된다.

---

## B-1 단계의 정체성

A 트랙이 "플래너 뇌에서 생각을 꺼내는 것"이었다면, B-1은 **"Claude Code가 혼자 결정하면 헤매거나 틀릴 지점을 플래너가 아닌 Claude(Projects)가 사전에 압축해두는 것"**이다.

이 정의는 두 가지를 함의한다:

**함의 1. B-1은 Claude 주도, 플래너는 결정 지점만 확인.**
A 트랙에서 플래너가 체계화를 리드했다면, B-1에서는 Claude가 A 산출물 읽고·웹 조사하고·스택 결정하고·초안 쓰는 게 디폴트다. 플래너는 "내 도메인 지식이 필요한 결정 지점"만 확인한다.

**함의 2. B-1 산출물의 1차 독자는 Claude Code다.**
A 산출물은 1차 독자가 플래너였다. B-1 산출물의 **1차 독자는 Claude Code**이다. 플래너는 §11(첫 프롬프트 복붙) §12(완료 보고) §6(LLM prompt 선택) 외에는 직접 읽지 않는다. 따라서 모든 섹션은 **Claude Code가 명확히 해석할 수 있도록** 작성. 추상 표현·완충어·암묵적 가정 금지.

**함의 3. 인프라 비종속 코드.**
B-1에서 짜는 모든 코드는 인프라로 올릴 때 구조를 바꾸지 않아야 한다. 12-Factor App 원칙 기반 13개 체크리스트로 자동 검증. 이 검증을 통과해야 B-2 진입 가능.

---

## 산출물 형태 (분량 제한 없음)

> **분량 구분**
> - **이 문서 (템플릿+워크플로우)**: 분량 제한 없음. 작성 가이드다.
> - **B-1 산출물**: 분량 제한 없음. AI가 읽고 구현할 문서다. 빠진 내용 없게 충실히. Claude Code 컨텍스트 윈도우에 들어갈 정도면 됨.

### 산출물 구조

B-1 산출물의 최종 형태는 아래 구조로 구성된다.

```
[맨 앞] 1차 독자 선언문 (1차 독자는 Claude Code, 플래너는 §1·§6·§7·§11·§12만 읽음)

0. A 트랙 요약 블록
   - A-0 핵심 가설
   - A-1 Phase 구조
   - A-2 화면 목록

1. 시작 전 체크리스트 (사람이 준비해야 할 것 — 로컬 한정)
2. 기술 스택 확정 + 1줄 이유 (로컬 우선, SaaS 금지)
3. 기술 리스크 사전 조사 (라이브러리 비교 + 추천 + Hold 플래그)
4. 프로젝트 구조 스켈레톤 (폴더·DB 스키마·환경변수)
5. 빌드 순서 (의존성 있는 순서)
6. LLM system prompt 초안 (복수, 플래너 선택용)
7. 플래너 결정 항목 ([SO 결정 필요] 마커)
8. Claude Code 재량 범위 (혼자 결정 OK 목록)
9. bypass 멈춤 트리거 (반드시 사람에게 물어야 할 것)
9.5. 실패·재시도 상한 (같은 실패 3회 룰 등)
10. 검증 방법 — 시나리오 PASS + 포터빌리티 13개 체크
11. Claude Code 첫 프롬프트 템플릿 (복붙 가능)
12. 완료 보고 포맷 (Claude Code가 세션 끝에 남길 것 — 13개 체크 포함)
13. Kill / Go / Hold 판정

[맨 끝] Claude의 적대적 압박 — "Kill할 이유 세 가지"
```

---

### 섹션별 상세

#### 0. A 트랙 요약 블록

A-0·A-1·A-2 산출물의 핵심을 맨 앞에 고정. Claude Code 세션 중간에 "지금 뭘 만들고 있는 거였지"를 잊지 않도록.

- **A-0 핵심 가설 한 문장** (A-0 §1 그대로)
- **A-1 Phase 구조 개요** (P0~PN 이름 + 1줄 설명)
- **A-2 화면 목록** (화면 이름 나열, 번호·포함 Step만)
- **MVP 경계 3개** (제품·타겟·목표 행동)

#### 1. 시작 전 체크리스트

**플래너가 Claude Code 세션 시작 전에 반드시 완료해둬야 할 것.** 로컬 한정 — 인프라 서비스 계정 금지.

bypass 모드는 계정 생성·이메일 인증·유료 티어 결제를 대신할 수 없다.

체크박스 형식:
```
[ ] <서비스> 계정 생성
    → 필요한 것: <구체 key/토큰 이름>
    → 발급 위치: <URL>
[ ] <도구> 설치 확인
    → 확인 명령: <cmd>
```

**필수 포함 카테고리 (B-1 한정 — 로컬 인프라만):**
- LLM API key (Anthropic·OpenAI·Gemini 등) — 로컬에서 LLM 호출 위해 필수
- Repo 관리 (GitHub 등) — 코드 버전 관리, 인프라 의존 아님
- 로컬 DB 도구 (SQLite는 zero-config, Postgres는 Docker)
- Node.js·Python 등 런타임 설치 확인
- Claude Code 설치 확인

**B-1에서 금지된 카테고리 (B-2로 미룸):**
- Vercel·Netlify·Cloudflare 등 배포 서비스
- Supabase·Neon·PlanetScale 등 SaaS DB
- Stripe·Paddle 등 결제
- 도메인·DNS

**꼭 마지막에 "필요한 key 전체 목록"을 별도 블록으로** — Claude Code가 환경변수 파일 템플릿 생성할 때 이 목록을 그대로 쓴다.

**좋은 예시:**
```
[ ] Anthropic API key 발급 (console.anthropic.com)
    → 크레딧 최소 $10 충전 (MVP 검증용, 100회 호출 예상)
```

**나쁜 예시:**
```
[ ] LLM 계정 만들기
```
이유: 어느 서비스인지·어떤 key가 필요한지 명시 안 됨. Claude Code가 결국 물어봄.

#### 2. 기술 스택 확정 + 1줄 이유

Claude Code가 "Next.js냐 Remix냐" 고민하지 않도록 스택 전체를 박아둔다. **각 선택에 1줄 이유**를 붙여야 플래너가 나중에 "왜 이걸 골랐지" 재검토 가능.

**B-1 스택 결정 원칙:**
1. **로컬 우선** — SaaS 종속성 0개
2. **인프라로 옮길 때 어댑터만 갈아끼우면 되는 구조** (12-Factor 백업 서비스 원칙)
3. MVP 속도 (세팅 시간 짧을 것)
4. Claude Code가 이미 익숙한 패턴
5. 무료 티어로 MVP 범위 소화 가능 여부 (LLM API 외엔 무료)

**좋은 예시:**
```
| 영역 | 선택 | 1줄 이유 |
| --- | --- | --- |
| 프레임워크 | Next.js 14 App Router | 표준 스택 + AI SDK 스트리밍 지원 |
| DB (로컬) | SQLite (better-sqlite3) | zero-config, 파일 하나, B-2에서 Postgres로 어댑터 교체 |
| ORM | Drizzle | SQLite·Postgres 모두 지원, 인프라 전환 시 코드 무변경 |
| UI | shadcn/ui + Tailwind | 가볍고 디폴트 디자인 양호 |
| LLM SDK | @anthropic-ai/sdk | 인프라 무관, 환경변수만 |
```

**나쁜 예시:**
```
Next.js + Supabase 씁니다.
```
이유: Supabase는 SaaS. B-1에서 금지. B-2에서 결정.

#### 3. 기술 리스크 사전 조사

Claude Code가 혼자 라이브러리 조사하면 30분~1시간씩 날린다. Claude(Projects)가 **먼저 웹 검색해서 후보 비교표와 추천**을 남긴다.

**조사 깊이 — 층위 2 기본:**
- 후보 2~3개 나열
- 각 후보의 주간 다운로드·유지보수 상태·라이선스·API 형태
- 추천 1개 + 이유

**층위 3 (실제 코드 검증)은 예외적으로만.** 핵심 가설 검증에 직결되는 항목만.

**Hold 신호 조항:** 조사 결과가 "라이브러리가 X를 보장하는지 불명확" 같은 상태로 끝나면 **Hold 플래그**를 섹션 7(플래너 결정 항목)에 명시. 플래너가 샘플 1개로 수동 검증 후 Go.

**조사 대상 카테고리 (프로젝트별 다름):**
- 핵심 도메인 라이브러리 (사주톡의 만세력, 그림 그리기 앱의 캔버스 라이브러리 등)
- ORM·DB 어댑터 (12-Factor 백업 서비스 원칙 만족 여부 확인)
- 인증 라이브러리 (로컬·인프라 모두에서 작동하는지)
- LLM SDK·스트리밍 라이브러리

#### 4. 프로젝트 구조 스켈레톤

**폴더 레이아웃**: 주요 디렉토리와 용도. 12-Factor 원칙 반영.

**DB 스키마 개요**: 테이블·주요 컬럼 (정확한 SQL은 Claude Code가 생성)

**환경변수 목록**: `.env.local` 템플릿. **모든 외부 의존(DB·LLM·인증)은 환경변수로 추상화** — B-2에서 환경변수 값만 바꿔 인프라로 전환 가능해야 한다.

**좋은 예시 (폴더 구조 — 사주톡):**
```
sajutalk/
├── app/                       ← Next.js App Router
│   ├── api/
│   │   ├── session/
│   │   ├── interpret/
│   │   └── qna/
│   └── (screens)/
├── lib/
│   ├── db/                    ← DB 어댑터 (SQLite ↔ Postgres 교체 가능)
│   │   ├── index.ts           ← 환경변수로 분기
│   │   ├── sqlite.ts
│   │   └── postgres.ts
│   ├── llm/                   ← LLM 어댑터 (provider 교체 가능)
│   ├── prompts/               ← system prompt 모음
│   └── manse/                 ← 도메인 로직 (만세력)
├── data/                      ← 로컬 SQLite 파일 (gitignore)
├── styles/
│   └── design-tokens.css      ← A-3 산출물에서 import
├── tailwind.config.js         ← A-3 산출물의 Tailwind config 적용
└── .env.local                 ← key 모음
```

**A-3 디자인 토큰 적용 (필수):**
A-3 산출물의 §4-b 디자인 토큰을 다음 위치에 적용:
- `styles/design-tokens.css` — CSS 변수 (--color-primary 등)
- `tailwind.config.js` — Tailwind 설정에 추가 (A-3 §4-b 그대로 복붙)
- `app/layout.tsx` 또는 `_app.tsx` — `import './styles/design-tokens.css'` 추가
- 폰트: Google Fonts 링크를 `<head>`에 추가 (A-3 §2-c에 명시된 폰트)

A-3에 Claude Design handoff bundle이 있으면 위 절차 대신 bundle을 unzip해 적용.

**환경변수 템플릿 좋은 예시 (.env.local):**
```bash
# LLM
ANTHROPIC_API_KEY=sk-...

# DB (로컬: sqlite, 인프라: postgres URL)
DATABASE_URL=sqlite:./data/saju.db
DATABASE_PROVIDER=sqlite

# 앱
PORT=3000
NODE_ENV=development
```

**Claude Code에게 주는 지시:** "위 스키마는 개요. 정확한 SQL migration과 마이그레이션 스크립트(`npm run migrate`)를 Claude Code가 생성. ORM(Drizzle)으로 SQLite·Postgres 호환 스키마 정의."

#### 5. 빌드 순서

**의존성 있는 순서**대로 단계 나열. Claude Code가 이 순서로 실행.

**좋은 예시 (사주톡):**
```
1. `npm create next-app sajutalk` → TypeScript·Tailwind·App Router 선택
2. `npx shadcn init` + 필요 컴포넌트 설치 (button, input, card)
3. .env.local 생성 + 섹션 1 key 목록 채우기
4. **A-3 디자인 토큰 적용** (styles/design-tokens.css + tailwind.config.js + 폰트 link)
5. lib/db/ 어댑터 셋업 (SQLite 디폴트, Postgres 인터페이스 동일)
6. Drizzle 스키마 정의 + 마이그레이션 스크립트
7. lib/manse/ 만세력 엔진 셋업 + Hold 플래그 검증 포인트
8. lib/llm/ Anthropic SDK 어댑터 + 스트리밍 헬퍼
9. lib/prompts/ 섹션 6 확정 prompt 저장
10. API routes: session·interpret·qna 순서로 스텁
11. 화면 1~2 (생시 입력·고민 입력) 구현 + E2E 확인 (A-3 디자인 적용)
12. 화면 3 (4지선다) — 섹션 7 [결정 2] 확정된 방식으로
13. 화면 4 (긴 해석 스트리밍) — useChat 연동, A-3 §3 디자인 의도 적용
14. 화면 5·6 (Q&A 루프) + 카운터 차감
15. 화면 7 (정리 응답)
16. 화면 8 (부모 생시 조건부)
17. localhost:3000에서 시나리오 1·2 E2E 검증
18. 포터빌리티 13개 체크리스트 자동 검증
19. 섹션 12 완료 보고 생성
```

각 단계 끝나면 한 줄 진행 보고 (예: `[5/18] ✓ Drizzle 스키마 정의 완료`).

#### 6. LLM system prompt 초안 (복수)

이 섹션은 **플래너가 선택해야 하는 것**. PART 1에 있다.

A 트랙에서 안 다뤘던 영역. LLM 내부 지침은 화면에 안 보이지만 검증 축(예: "내 얘기네" 체감)에 결정적이다. Claude가 복수 초안 쓰고 플래너가 섹션 7에서 선택.

**원칙:**
- 2~3개 초안 제시 (톤이 서로 구분되게)
- 각 초안에 "언제 이게 맞는가" 한 줄
- Claude 추천 + 이유

**예시 구조 (톤만. 내용은 프로젝트별):**
```
초안 A: [톤 이름]
...prompt 본문...

초안 B: [톤 이름]
...prompt 본문...

초안 C: [톤 이름]
...prompt 본문...

Claude 추천: <A|B|C>
이유:
1. A-0 ___ 명시됨
2. ___가 핵심 가설 ___와 직결
3. ___
```

**초안 수: 2~3개.** 1개면 선택이 아님. 4개 넘으면 플래너 부담.

**프로젝트에 prompt가 여러 개 필요하면** (예: 긴 해석·Q&A·정리 3개), 각각 섹션 6-a, 6-b, 6-c로 나눠서 각각 복수 초안.

#### 7. 플래너 결정 항목 ([SO 결정 필요])

정책 v11 원칙 5 적용. 옵션 + 추천 + 이유 필수.

**포함할 것:**
- 섹션 6 prompt 초안 중 선택
- 섹션 3 Hold 플래그 해소
- 기타 프로젝트별 결정 (예: 4지선다 사전 작성 vs 즉석 생성)

**B-1에서 금지 (B-2로 미룸):**
- 배포 서비스 선택
- 도메인 결정
- 공개 범위 결정

**형식 (A 트랙과 동일):**
```
[SO 결정 필요] N: [한 줄 질문]
- 옵션 A: [구체 안] — 장단점
- 옵션 B: [구체 안] — 장단점
- Claude 추천: [A 또는 B]
- 이유: [핵심 가설 또는 MVP 경계와 연결]
```

#### 8. Claude Code 재량 범위

**"물어보지 말고 혼자 결정하세요" 목록.** 이게 없으면 Claude Code가 사소한 것도 멈추고 물어봄 → bypass 모드 의미 상실.

**디폴트 포함 (B-1):**
- UI 컴포넌트 선택 (shadcn/ui 내에서)
- 파일·폴더 이름 세부 (섹션 4 구조 안에서)
- CSS 세부 (간격·색상 숫자)
- 에러 UI 문구 (MVP 수준)
- git commit 메시지
- 의존성 버전 (major는 섹션 2 고정, minor/patch 자유)
- 주석 스타일
- 테스트 작성 여부 (E2E는 섹션 10, 단위 테스트는 재량)
- 로컬 DB 파일 위치 (`./data/` 안에서)

#### 9. bypass 멈춤 트리거

**이런 일 발생하면 멈추고 사람에게 묻는다.** B-1은 로컬이라 비가역 작업이 적지만 그래도 명시.

**B-1 기본 멈춤 트리거 4개:**
1. **API key가 코드에 하드코딩될 위험** — 환경변수가 아닌 곳에 key 쓰려 할 때
2. **`.git/`·`.env`·`node_modules/` 외 파일 삭제** — 사용자 데이터 의도치 않은 삭제 방지
3. **재시도 상한 도달** (§9.5) — 같은 실패 3회 반복
4. **시나리오 PASS 안 됨에도 "성공" 선언하려 할 때** — 검증 결과 위조 방지

**프로젝트별 추가:** Claude Code가 빌드 중에 "이걸 결정해도 되나" 망설이는 지점이 있으면 추가.

**멈출 때 형식:**
```
[멈춤] <트리거 번호>
이유: <한 줄>
시도한 것: <목록>
Eugene 결정 필요: <A|B|C 옵션>
```

#### 9.5. 실패·재시도 상한

**같은 실패 3회 룰:** Claude Code가 같은 작업·같은 에러로 3회 실패하면 정지. Eugene 호출.

**디폴트:**
- 같은 명령 3회 실패 → 정지
- 같은 의존성 설치 3회 실패 → 정지
- 같은 테스트 3회 FAIL → 정지

**예외:**
- 네트워크 일시 오류는 5회까지 재시도 (지수 백오프)

**정지 시 보고 형식:**
```
[정지] §9.5 재시도 상한
시도한 것: <3가지>
실패 양상: <공통 증상>
추측 원인: <Claude Code의 추정>
Eugene 의견 필요
```

#### 10. 검증 방법 — 시나리오 PASS + 포터빌리티 13개 체크

**B-1의 검증은 두 단계:**

**단계 1. 시나리오 PASS (A-1 §4 시나리오를 localhost에서 E2E로)**

A-1 §4 시나리오를 E2E로 통과시킨다.

형식:
```
[검증 1] A-1 시나리오 1: <한 줄 요약>
  실행 방법: <수동 / Playwright 자동>
  PASS 조건: <구체적 관찰 항목>
  - 관찰 1: ___
  - 관찰 2: ___
  - 관찰 3: ___

[검증 2] A-1 시나리오 2: ...
```

**수동 vs Playwright 선택:**
- 시나리오에 사용자 타이핑이 많고 분기가 많으면 Playwright가 편함
- MVP 단계·시나리오 2~3개는 수동이 더 빠름
- **Claude 디폴트 추천: 수동**

**PASS 조건은 구체적으로.** "정상 작동" 같은 추상 금지.

**단계 2. 포터빌리티 13개 체크리스트 (12-Factor App 기반 + 서버리스 호환성 추가)**

**Claude Code가 빌드 끝에 자동 점검**. 각 항목 PASS/FAIL을 §12 완료 보고에 기록.

```
[1] 코드베이스 (1 codebase, many deploys)
    - 단일 git repo에서 로컬·인프라 모두 배포 가능?
    - 환경별 다른 코드 브랜치 없음?
    - 검증: `git remote -v`로 단일 origin + 환경별 브랜치 없음

[2] 의존성 명시·격리
    - package.json/requirements.txt에 모든 의존성 명시?
    - 시스템 전역 패키지 의존하지 않음?
    - 검증: 깨끗한 디렉토리에서 `npm ci`만으로 실행 가능

[3] 환경변수 분리
    - API key·DB URL이 코드에 하드코딩 안 됨?
    - .env.local과 .env.production 분리?
    - 검증: grep으로 하드코딩된 URL·key 0개

[4] 백업 서비스 어댑터
    - DB·캐시·LLM API·스토리지가 어댑터 패턴으로 추상화?
    - lib/db/index.ts 같은 단일 진입점에서 환경변수 분기?
    - 검증: 직접 SDK 호출 없이 추상화 계층 통과

[5] 빌드/릴리스/실행 분리
    - npm run build로 빌드 산출물 생성 가능?
    - 빌드 산출물 + 환경변수만으로 어디서든 실행 가능?
    - 검증: dist/ 또는 .next/ + .env로만 실행 가능

[6] 무상태 프로세스
    - 세션·캐시가 인메모리에 저장 안 됨?
    - 사용자 데이터는 DB·외부 캐시에 저장?
    - 검증: 모듈 레벨 mutable 상태 grep 0개

[7] 포트 바인딩
    - 앱이 자체적으로 HTTP 서버 노출 (Apache/Nginx 의존 없음)?
    - PORT 환경변수로 포트 변경 가능?
    - 검증: PORT=8080 npm start로 다른 포트 실행 가능

[8] 동시성 (수평 확장 가능)
    - 백그라운드 작업이 별도 프로세스로 분리?
    - setInterval·인메모리 cron 없음?
    - 검증: 코드에 setInterval 같은 인메모리 스케줄러 0개

[9] 빠른 시작·정상 종료
    - 앱이 10초 이내 시작 가능?
    - SIGTERM 시그널로 graceful shutdown?
    - 검증: time npm start 측정 + kill -TERM 정상 종료 확인

[10] 개발-운영 동등성
    - 로컬 DB와 인프라 DB가 호환 가능 (둘 다 SQL)?
    - 외부 API·LLM이 로컬·인프라 동일 엔드포인트?
    - 검증: 로컬 전용 라이브러리만 의존 안 함 (sqlite3만 있고 pg 없으면 FAIL)

[11] 로그를 이벤트 스트림으로
    - 로그가 stdout으로 나감 (파일에 직접 안 씀)?
    - console.log만 씀?
    - 검증: fs.writeFile로 로그 쓰는 부분 0개

[12] 관리 작업 일회성
    - DB 마이그레이션이 일회성 스크립트?
    - 시드 데이터 주입이 일회성?
    - 검증: package.json scripts에 migrate·seed 항목 존재

[13] 서버리스 런타임 호환성
    - Edge Runtime 가정 코드가 Node.js 호환 모드로도 실행?
    - Lambda 콜드스타트 가정해 글로벌 변수에 의존 안 함?
    - 파일시스템 쓰기를 /tmp 외 경로에 안 함?
    - 검증: fs.writeFile 사용 시 모두 /tmp 또는 환경변수 경로
```

**13개 검증 결과 형식:**
```
| # | 항목 | PASS/FAIL | 이슈 |
|---|------|-----------|------|
| 1 | 코드베이스 | PASS | - |
| 2 | 의존성 명시 | PASS | - |
| ... | ... | ... | ... |
```

**FAIL이 1개라도 있으면**: §12 완료 보고에 명시. 플래너 판단으로 (a) Claude Code에게 수정 지시 (b) 그대로 B-2 진입 (그 항목 인지하고 인프라에서 해결).

#### 11. Claude Code 첫 프롬프트 템플릿

**플래너가 복붙할 수 있는 상태로.** 경로·버전·프로젝트명은 빈칸으로 두되 주석으로 채울 자리 표시.

**표준 구조:**
```
[프로젝트명] MVP를 로컬에서 만든다.

[컨텍스트]
다음 파일들을 순서대로 읽고 실행 계획을 수립한다:
1. <A-0 경로>
2. <A-1 경로>
3. <A-2 경로>
4. <A-3 경로>  ← 디자인 토큰 + 핵심 화면 디자인 의도
5. <B-1 경로>  ← 이 파일이 실행 지침서

[동작 모드]
--dangerously-skip-permissions
B-1 산출물 §8 재량 범위 안에서는 스스로 결정.
§9 멈춤 트리거 해당 시 정지하고 사람에게 질문.
§9.5 재시도 상한 넘으면 정지.

[목표]
B-1 산출물 §10 검증 방법 통과:
1. 시나리오 1·2 PASS (localhost:3000에서)
2. 포터빌리티 13개 체크 (자동 점검 후 결과 기록)

[제약]
인프라 SaaS(Vercel·Supabase·Netlify 등) 사용 금지.
모든 의존성은 로컬 또는 LLM API뿐.

[시작]
§5 빌드 순서대로 실행. 각 단계 끝나면 한 줄 진행 보고.
§12 완료 보고 포맷에 맞춰 최종 정리.
```

#### 12. 완료 보고 포맷

**Claude Code가 세션 끝에 남길 마크다운 포맷.** 플래너가 수동 스캔 없이 3분 안에 상황 파악하게.

**표준 구조:**
```markdown
# [프로젝트명] MVP 로컬 빌드 완료 보고

## 최종 산출물
- 로컬 실행 명령: `npm run dev`
- localhost:3000 접속 시 시나리오 PASS 확인 가능
- GitHub repo: <URL>

## 완료된 것
- [체크 리스트: §5 빌드 순서 각 항목 ✓/◐/✗]

## 미완료 / 제한 사항
- 무엇이 안 됐는가
- 부분 구현 된 것

## 시나리오 검증 결과 (§10 단계 1)
- 시나리오 1: PASS / FAIL / 부분
  - 관찰 1: ...
  - 관찰 2: ...
- 시나리오 2: ...

## 포터빌리티 13개 체크 결과 (§10 단계 2)
| # | 항목 | PASS/FAIL | 이슈 |
|---|------|-----------|------|
| 1 | 코드베이스 | PASS | - |
| 2 | 의존성 명시 | PASS | - |
| 3 | 환경변수 분리 | PASS | - |
| 4 | 백업 서비스 어댑터 | PASS | - |
| 5 | 빌드/릴리스/실행 분리 | PASS | - |
| 6 | 무상태 프로세스 | PASS | - |
| 7 | 포트 바인딩 | PASS | - |
| 8 | 동시성 | PASS | - |
| 9 | 빠른 시작·정상 종료 | PASS | - |
| 10 | 개발-운영 동등성 | PASS | - |
| 11 | 로그 이벤트 스트림 | PASS | - |
| 12 | 관리 작업 일회성 | PASS | - |
| 13 | 서버리스 호환성 | PASS | - |

총 PASS: 13/13 (또는 N/13)

## 발생한 이슈
- §9.5 상한에 걸린 지점 (있으면)
- 재시도로 해결한 것

## 다음 세션에 필요한 것 (B-2 진입 전)
- Eugene의 결정이 필요한 남은 지점
- 추가 작업 리스트
- FAIL 항목이 있으면 그 영향 분석

## 리소스 사용
- LLM API 크레딧: 추정 $X
```

#### 13. Kill / Go / Hold 판정

A 트랙과 동일.

- **Go** — Claude Code bypass 세션 진입. 근거 3줄.
- **Kill** — 여기서 종료. 근거 3줄. (B-1 단계 Kill은 "A 기획이 아직 빌드 가능한 상태가 아니다"라는 뜻. A-2로 회귀.)
- **Hold** — 특정 외부 확인 후 재판정. 무엇을 확인해야 하는지 명시. 최대 2회.

**B-1 단계 Hold이 흔한 이유:**
- 섹션 3 기술 리스크 조사 결과 중 Hold 플래그 (예: 만세력 검증)
- 섹션 1 체크리스트에 플래너가 아직 준비 안 한 계정
- 섹션 7 결정 항목에 플래너가 아직 답 안 함

이 셋 중 하나라도 미해결이면 Go 불가. Hold.

---

### 산출물 마지막 (Claude의 적대적 압박)

정책 v11 역할 1. Claude가 자동으로 맨 아래에 "지금 이 빌드 지침서를 Kill할 이유 세 가지" 첨부. B-1 단계 Kill 이유는 **빌드 지침서의 구조적 결함**을 찌른다.

예시:
- "섹션 3 만세력 라이브러리 추천이 3년 전 유지보수 멈춤. 버그 시 Claude Code 혼자 수정 불가"
- "섹션 9 멈춤 트리거 4개가 프로젝트 특화 리스크(예: 사용자 개인정보 수집) 누락"
- "섹션 10 시나리오 PASS 조건이 수동 관찰 의존. Claude Code가 시나리오 '통과'를 선언해도 사실이 아닐 가능성"

---

## 워크플로우 (채우는 순서)

B-1 산출물 작성은 **Claude 주도, 플래너는 결정 항목 확인**. A 트랙과 달리 플래너 턴 수가 적다.

### STEP 0: 시작 선언 및 A 산출물 확인

**플래너의 행동:**
- `B-1 시작` 선언 (Claude의 턴 카운터 개시)
- A-0·A-1·A-2 산출물 첨부 (또는 같은 대화창에서 이어서 진행)

**Claude의 행동:**
- 턴 카운팅 시작
- A-2가 Go 판정으로 확정되지 않았으면 B-1 진입 불가 → "A-2 먼저 마무리 필요" 알림
- A-0/A-1/A-2 핵심 (가설·MVP 경계·화면 목록) 추출해 §0 작성

### STEP 1: 기술 스택 + 사전 조사 제시 (번들링)

**Claude의 행동:**
- A-1/A-2에서 기술 요건 추출 (DB 필요 여부·스트리밍·외부 API 등)
- **웹 검색으로 라이브러리·패턴 조사** (층위 2)
- §2 기술 스택 확정 표 + §3 리스크 조사 결과 한 응답에 번들
- §3에 Hold 플래그가 있으면 명시
- **로컬 우선 원칙 확실히 — SaaS 0개**

**플래너의 행동:**
- §2 스택 확인 (반대 있으면 지적)
- §3 Hold 플래그 확인 (나중에 해소)
- OK면 다음

### STEP 2: 플래너 직접 항목 (§1·§6·§7) 번들 제시

**Claude의 행동:**
- §1 시작 전 체크리스트 작성 (로컬 한정)
- §6 LLM system prompt 복수 초안 작성 (2~3개)
- §7 플래너 결정 항목 정리 ([SO 결정 필요] 마커)

한 응답에 번들. 플래너가 한 번에 답하도록.

**플래너의 행동:**
- §1 체크리스트에 빠진 항목 있으면 지적
- §6 prompt 초안 중 선택 (또는 수정 지시)
- §7 [SO 결정 필요] 항목에 일괄 답변
- §3 Hold 플래그도 이 턴에 해소

### STEP 3: Claude Code 참조 항목 (§4·§5·§8·§9·§9.5·§10·§11·§12) 번들 제시

**Claude의 행동:**
- §4 프로젝트 구조·스키마·환경변수 작성 (12-Factor 반영)
- §5 빌드 순서 작성 (포터빌리티 검증 단계 포함)
- §8 재량 범위 작성
- §9 멈춤 트리거 작성 (+ 프로젝트 특화 추가 있으면)
- §9.5 재시도 상한 작성
- §10 검증 방법 작성 (시나리오 + 13개 체크)
- §11 Claude Code 첫 프롬프트 템플릿 작성 (경로 자동 채움)
- §12 완료 보고 포맷 작성 (13개 체크 표 포함)

분량 많으면 2~3 응답으로 쪼개도 됨. 단 "이번 응답은 §4·§5·§8, 다음은 §9·§9.5·§10, 그다음은 §11·§12" 식으로 명시.

**플래너의 행동:**
- 각 섹션 확인·수정 지시

### STEP 4: 전체 조립 + Kill할 이유 3개 + 판정 추천

**Claude의 행동 (필수):**
- 전체 B-1 산출물 클린 조립
- 맨 아래에 **"Kill할 이유 세 가지"** 추가
- Go/Kill/Hold 추천 + 근거 3줄

**플래너의 행동:**
- Claude 추천대로 확정하거나 다른 판정

### STEP 5: Go 확정 시 자동 파일 저장

**Claude의 행동:**
- 파일명: `04_B-1_로컬에서만들기_[프로젝트명].md`
- 저장 위치: `/mnt/user-data/outputs/`
- 변경 가시화 마크 모두 제거한 클린본

Go 후 플래너에게 전달할 메시지:
```
B-1 산출물 저장 완료. 다음 단계:
1. §1 시작 전 체크리스트의 미완료 항목 완료
2. §11 Claude Code 첫 프롬프트 템플릿 복사
3. 로컬 빈 디렉토리에서 claude --dangerously-skip-permissions 실행
4. 프롬프트 붙여넣기
5. Claude Code가 진행 시작. 멈춤 트리거 걸리면 응답.
6. §12 완료 보고 받기 — 시나리오 PASS + 13개 체크 결과 확인.

13개 체크 결과 검토 후:
- 모두 PASS → B-2 진입 준비 완료
- FAIL 일부 있음 → 영향 분석 후 B-2 진입 또는 수정 지시
```

---

## 턴 관리

B-1 산출물의 턴 상한은 **5턴**. 70% 경고는 **4턴째**. 이유:
- Claude 주도라 플래너 턴 소비가 적음
- 대부분의 작성이 Claude가 알아서 하고, 플래너는 확인·결정만
- A-0(5턴)과 비슷한 수준

### 일반적인 턴 소비 예상

| 턴 | 플래너 메시지 | Claude 응답 |
| --- | --- | --- |
| 1 | `B-1 시작` + A 산출물 | §0 요약 + §2 스택 + §3 조사 결과 (Hold 플래그 포함) |
| 2 | 스택 확인 + Hold 해소 | §1 체크리스트 + §6 prompt 초안 + §7 결정 항목 |
| 3 | §6 선택 + §7 일괄 답변 | §4·§5·§8·§9·§9.5·§10·§11·§12 (분량 많으면 2~3 응답) |
| 4 | 세부 조정 | 클린 조립본 + Kill 이유 3개 + 판정 추천 |
| 5 | Go 확정 | 자동 파일 저장 + 다음 단계 안내 |

5턴 초과 시 Kill 이유 3개, 10턴 초과 시 강한 Kill 권고.

---

## B-1 산출물에서 흔한 실패 모드

**실패 1: 기술 스택에 SaaS 포함**
- 예: §2에 Supabase·Vercel
- 왜 나쁜가: B-1은 로컬 한정. SaaS는 B-2 영역
- 교정: 로컬 도구로 교체 (SQLite, 로컬 실행)

**실패 2: 사전 조사가 층위 3로 감 (깊이 초과)**
- 예: 라이브러리 3개를 실제 설치해서 10개 샘플 돌림
- 왜 나쁜가: B-1 작성에 1시간 넘게 쓰면 비용 대비 가치 감소
- 교정: 층위 2 기본. 층위 3 승격은 "검증 축 직결 + Hold 불가피" 두 조건 모두 만족 시만

**실패 3: 플래너 결정 항목이 "초안 떠넘기기" 패턴**
- 예: `[SO 결정 필요] 만세력 어느 거 쓰죠?` (옵션·추천 없이)
- 왜 나쁜가: 정책 v11 원칙 5 위반
- 교정: 옵션 2~4개 + Claude 추천 + 이유 필수

**실패 4: Claude Code 재량 범위가 불명확**
- 예: "자잘한 건 알아서"
- 왜 나쁜가: 실제 bypass에서 Claude Code가 여전히 물어봄
- 교정: 행동 규칙 목록 형태로 구체화

**실패 5: 멈춤 트리거가 추상적**
- 예: "위험한 일 하면 멈춤"
- 왜 나쁜가: Claude Code가 "위험"을 판정 못 함 → 안 멈춤
- 교정: 구체적 행동 4가지로

**실패 6: 재시도 상한 누락**
- 왜 나쁜가: 같은 실패 30번 반복하며 플래너 호출 안 함
- 교정: §9.5 디폴트 3개 룰 박기

**실패 7: 검증 방법이 "작동함" 수준**
- 왜 나쁜가: Claude Code가 "통과"를 선언해도 실제 통과 판정 불가
- 교정: A-1 §4 시나리오 기반 + PASS 조건 구체 관찰 항목 3~5개

**실패 8: 포터빌리티 13개 체크 누락 또는 표면적 검증**
- 예: "PASS"라고만 적고 실제 검증 명령 안 실행
- 왜 나쁜가: B-2에서 인프라 올릴 때 구조 변경 필요 → 분리 의미 상실
- 교정: 각 항목에 실제 검증 명령 명시 (grep·time·실행 명령 등)

**실패 9: 환경변수 추상화 미흡**
- 예: API 호출 코드에 `https://api.anthropic.com` 하드코딩
- 왜 나쁜가: 12-Factor [3] 환경변수 분리 위반. B-2 전환 시 코드 수정
- 교정: 모든 외부 의존을 환경변수로

---

## B-2 진입 조건 (Go 판정 시)

B-1이 Go로 끝나면 B-2 진입 가능. **다음 셋 모두 만족 시만:**

1. **B-1 산출물 파일이 저장됨** (`04_B-1_로컬에서만들기_[프로젝트명].md`)
2. **Claude Code 빌드 완료** (시나리오 PASS + 13개 체크 결과)
3. **§12 완료 보고가 작성됨** (B-2 입력으로 사용)

이 셋 중 하나라도 미충족이면 B-2 진입 불가.

**13개 체크 FAIL 항목 처리:**
- 모두 PASS → 즉시 B-2 진입 가능
- FAIL 1~3개 → 플래너가 영향 분석 후 결정 (수정 지시 또는 그대로 B-2)
- FAIL 4개 이상 → B-1으로 회귀 권장 (포터빌리티가 너무 깨져 있음)

---

## 도구 환경

**기본 환경: Claude Projects.** B-1 산출물도 Projects에서 작성. 이유는 웹 검색·리서치가 Projects의 강점이기 때문이다. B-1은 특히 웹 검색(기술 리스크 사전 조사) 의존도가 A보다 높음.

**Claude Code는 B-1 산출물 작성의 다음 단계.** B-1 작성 중에는 Claude Code 안 씀.

---

## 변경 이력

- **v2** (2026-04-25) — 정책 참조 v10 → v11. 입력에 A-3 산출물 추가. 파일 번호 04 → 05. §4 디자인 토큰 적용 단계, §5 빌드 순서에 디자인 적용 추가.
- **v1** (2026-04-23) — 최초. 정책 v11 및 사주톡 A 트랙 실전 경험을 토대로 작성. B 빌드지침서 v1을 분할해 B-1(로컬 빌드)로 재정의. 12-Factor App 기반 포터빌리티 13개 체크리스트 신설 — Claude Code가 빌드 끝에 자동 검증해 B-2 진입 조건으로 사용. 인프라 SaaS 의존 0개 원칙 명시.
