# B. 사주톡 — 빌드 지침서

> **이 문서의 1차 독자:**
> - **PART 1** — Eugene (플래너)이 직접 읽고 행동
> - **PART 2** — Claude Code가 `--dangerously-skip-permissions` 세션에서 참조

---

## §0. A 트랙 요약 블록

**A-0 핵심 가설**
이직·연애·결혼 고민이 있는 28~34세 여성이, 링크로 받은 웹 사주 서비스에서, 긴 해석을 읽은 뒤 스스로 최대 3번의 추가 질문을 써가며 대화를 완주할 것이다.

**A-1 Phase 구조**
- P0 진입 & 재료 수집 (생시·고민·반복 패턴 답)
- P1 긴 해석 응답 (만세력 주입 + 5~8문장 스트리밍)
- P2 사용자 주도 Q&A 루프 (최대 3회)
- P3 정리 & 저장 & 조건부 요청 (3번 다 쓴 사용자에게 부모 생시 요청)

**A-2 화면 목록**
- 화면 1 생시 입력 (첫 세션만)
- 화면 2 고민 입력 (4지선다 카드 + 직접 쓰기)
- 화면 3 반복 패턴 4지선다
- 화면 4 대화 화면 (상태 A~F, 상단 카운터 없음)
- 화면 5 조건부 부모 생시/환경 요청 (화면 4 정리 아래 확장)

**MVP 경계**
- 제품: AI 대화형 사주 해석 웹서비스
- 타겟: 28~34세 여성, 수도권, 이직·연애·결혼 고민, Eugene 지인 10명
- 목표 행동: 3번 질문 다 쓰고 정리까지 완주 (보조: 1번 이상 질문 후 완주)

---

# PART 1 — Eugene이 직접 읽고 행동

## §1. 시작 전 체크리스트

```
[ ] Anthropic API key 발급 (console.anthropic.com)
    → 키 이름: ANTHROPIC_API_KEY
    → 크레딧 최소 $20 충전 (MVP 검증 + Playwright 테스트용)

[ ] GitHub 계정 + 새 private repo 생성
    → repo 이름: sajutalk (또는 원하는 이름)
    → 초기 상태: 빈 repo

[ ] Supabase 계정 + 새 프로젝트 생성
    → 프로젝트 이름: sajutalk
    → region: Northeast Asia (Seoul) 또는 Tokyo
    → 저장할 값: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY

[ ] Vercel 계정 + GitHub 연동
    → Vercel 대시보드에서 GitHub repo import 권한 허용
    → 배포 시 환경변수 수동 입력 예정

[ ] Node.js 18+ 설치 확인 (권장: 20 LTS)
    → 확인: node --version

[ ] .env.local 파일 위치 확정
    → 로컬: 프로젝트 루트/.env.local (gitignore됨)
    → Vercel: 대시보드 Settings > Environment Variables

[ ] Playwright 실행 환경 확인
    → 확인: npx playwright --version (없으면 Claude Code가 설치)
```

**필요한 환경변수 전체 목록** (.env.local 템플릿):
```
ANTHROPIC_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

---

## §6. LLM System Prompt (Eugene 확정본: 초안 B — 친근한 TV 역술가)

세 종류 프롬프트 모두 같은 "친근한 TV 역술가 페르소나"를 공유하되 태스크만 다름.

### §6-a. 긴 해석 프롬프트 (S6)

```
당신은 TV 사주 프로그램에 자주 나오는 유명 역술가입니다.
사용자의 만세력 원국과 고민, 반복 패턴 답을 받아 5~8문장의 해석을 씁니다.

톤: 따뜻하고 감정적. 공감 표현 자연스럽게 섞음
    ("아이고, 그랬구나 싶네요", "그럴 수밖에 없었겠어요").
    존대 유지하되 거리감 없음. 역술가가 "어머 맞죠?" 물어보는 느낌.

명리 용어 처리: 원국의 핵심 구조는 원어 한 번 노출
    ("갑인 일주예요 — 쉽게 말하면...") 후 일상어로 풀어씀.
    용어 설명은 길지 않게 한 문장으로.

구조: (1) 이름 부르며 따뜻하게 시작 ("○○님,")
    (2) 원국 핵심을 구체 상황 비유로 1~2문장
    (3) 반복 패턴 답을 원국에 연결해서 "이게 왜 그런지" 3~4문장 설명
    (4) 공감 한 줄 + 현재 고민에 대한 관점 1문장.

금지: 차가운 분석 톤. "단정지으면"·"결론적으로" 류.
    대신 "~이 나와요", "~인 타입이세요" 같은 열린 서술.

이전 세션 DB가 있으면 "아 지난번에 ○○ 고민 얘기하셨었죠. 그때..."
    자연스럽게 기억하는 말투.
```

### §6-b. Q&A 답변 프롬프트 (S9)

```
[§6-a 톤 그대로] + 아래 규칙 추가:

- 사용자 질문에 직접 답변. 원 해석과 모순 없이 일관성 유지
- 만세력 원국 + 반복 패턴 답 + 이전 대화 전체 맥락을 재료로 사용
- 5~7문장. 질문 성격에 따라 조금 더 길어도 됨
- Sycophancy 금지: 사용자가 원하는 답 아니어도 원국 근거 있으면 그대로 말함
  예: "이직이 맞냐" → "지금 이직은 원국상 ~라는 이유로 무리예요"도 OK

- 2회차 세션 + 이번이 2번째 질문 + 50% 조건이면 답변 말미에
  "혹시 이런 경험 있으세요?" 4지선다 4개 삽입. 답변 본문과 구분선.
```

### §6-c. 정리 응답 프롬프트 (S11)

```
[§6-a 톤 그대로] + 아래 규칙 추가:

- 지금까지 대화 맥락 전체를 한 번 더 묶어 정리
- 밀도는 질문 횟수 비례:
  * 0회 (이제 됐어요 바로): 3~4문장 짧게
  * 1~2회: 5~7문장 중간 밀도
  * 3회 다 씀: 8~12문장 풍부하게. 누적된 Q&A 답까지 종합
- 자동 전환(3번 다 씀)으로 진입하면 맨 앞 1문장에
  "세 번 물어봐주셨으니, 오늘 얘기 정리해드릴게요" 전환 문구
- 마지막 1문장: 따뜻한 맺음말 ("이번 해석이 도움 되셨길 바라요" 류)
- 금지: "다음에 또 오세요" 류 재방문 유도 (화면 5에서 별도 처리)
```

### §6-d. 고민 영역 분류 프롬프트 (S3)

```
사용자의 고민 텍스트를 네 영역 중 하나로 분류:
"이직", "연애", "결혼", "기타"

규칙:
- 한 단어로만 응답 (JSON·설명 불필요)
- 일·직장·직업 관련 → "이직"
- 사귀는 사람·데이트·썸·짝사랑 → "연애"
- 결혼·청혼·배우자 선택 → "결혼"
- 위 셋 어디에도 안 맞으면 → "기타"

예:
"이직을 할지 말지 고민이에요" → 이직
"결혼 얘기 나왔는데 확신이 안 서요" → 결혼
"아이 성적이 안 올라요" → 기타
```

---

## §7. 플래너 결정 항목 (모두 해결됨)

- **결정 1** (§6-a 톤): B 친근한 TV 역술가 — Eugene 확정
- **결정 2** (사용자 식별): A 익명 세션 — localStorage·쿠키 기반 uuid
- **결정 3** (배포 공개 범위): C Vercel 자동 도메인 (sajutalk.vercel.app)
- **결정 4** (50% 랜덤 4지선다): A 50% 유지 — A-2 기획 반영
- **결정 5** (만세력 검증): Playwright로 10건 자동 검증 (포스텔러 대조) — Eugene 확정

---

## §11. Claude Code 첫 프롬프트 템플릿

Eugene이 복붙할 상태. 파일 경로만 교체.

```
사주톡 MVP를 만든다.

[컨텍스트 — 아래 순서대로 읽고 실행 계획 수립]
1. /path/to/01_A-0_생각을_문서로_사주톡.md
2. /path/to/02_A-1_문서를_프로세스로_사주톡.md
3. /path/to/03_A-2_프로세스를_화면으로_사주톡.md
4. /path/to/04_B_빌드지침서_사주톡.md  ← 이 파일이 실행 지침서

[동작 모드]
--dangerously-skip-permissions
B 산출물 §8 재량 범위 안에서는 스스로 결정.
§9 멈춤 트리거 해당 시 정지하고 Eugene에게 질문.
§9.5 재시도 상한 넘으면 정지.

[최우선 순서]
B §5 빌드 순서 8번(만세력 Playwright 검증)을 다른 UI 작업보다 먼저 실행.
10/10 일치 실패 시 즉시 정지.

[목표]
B §10 검증 방법 통과. 배포 URL을 STDOUT에 최종 출력.

[시작]
§5 빌드 순서대로 실행. 각 단계 끝마다 한 줄 진행 보고
("[N/20] ✓ 단계명" 또는 "[N/20] ⏸ 정지 사유").
§12 완료 보고 포맷에 맞춰 최종 정리.
```

---

## §12. 완료 보고 포맷

Claude Code가 세션 끝에 출력할 마크다운.

```markdown
# 사주톡 MVP 빌드 완료 보고

## 최종 산출물
- 배포 URL: https://sajutalk.vercel.app
- GitHub repo: [URL]
- Supabase 프로젝트: [URL]

## 완료된 것 (§5 빌드 순서 20단계)
[1/20] ✓ Next.js 초기화
[2/20] ✓ shadcn init
...
[8/20] ✓ 만세력 검증 — 10/10 일치
...
[20/20] ✓ 완료 보고 생성

## 미완료 / 제한 사항
- 무엇이 안 됐는가
- 부분 구현된 것

## 검증 결과 (§10 기준)
- [검증 0] 만세력 10건: PASS (10/10 일치)
- [검증 1] 시나리오 1: PASS / FAIL / 부분
  - 관찰 1: ...
- [검증 2] 시나리오 2: PASS / FAIL / 부분
  - 50% 랜덤 테스트: 5회 중 N회 발동

## 발생한 이슈
- §9.5 상한 걸린 지점 (있으면)
- 재시도로 해결한 것

## 다음 세션에 필요한 것
- Eugene의 결정이 필요한 지점
- 추가 작업 리스트

## 리소스 사용
- Anthropic 크레딧 소비: 약 $X
- Supabase·Vercel: Free tier 내 / 초과
```

---

# PART 2 — Claude Code가 실행 중 참조

## §2. 기술 스택 확정

| 영역 | 선택 | 1줄 이유 |
|---|---|---|
| 프레임워크 | Next.js 14 App Router | Vercel 원클릭 배포 + Anthropic AI SDK 스트리밍 네이티브 |
| 언어 | TypeScript | 상태 머신 타입 안전성. 6상태 분기 버그 조기 발견 |
| DB·Auth | Supabase (Postgres + RLS) | 세션 누적 DB에 적합. 만세력 캐시 jsonb |
| UI | shadcn/ui + Tailwind | A-2 ASCII 와이어프레임 → 컴포넌트 1:1 매핑 쉬움 |
| LLM | Anthropic Claude Sonnet 4.6 (streaming) | 역술가 톤 + 한국어 품질 + 만세력 JSON 통합 안정적 |
| 만세력 엔진 | `@fullstackfamily/manseryeok` | 한국 특화, 진태양시 자동 보정, MIT 라이선스 |
| 배포 | Vercel | Next.js 네이티브, preview deploy |
| Repo | GitHub | Vercel 자동 배포 연동 |

**스킵 (MVP 밖)**: 인증(익명 세션만), 결제, 모바일 앱

---

## §3. 기술 리스크 사전 조사

### 리스크 1: 만세력 계산 엔진 (⚠️ 검증 축 직결)

후보 3개 비교:

| 라이브러리 | 한국 특화 | 진태양시 보정 | 라이선스 | 비고 |
|---|---|---|---|---|
| `@fullstackfamily/manseryeok` | ✅ | ✅ (내장) | MIT | 1000~2050 범위, DB 없는 순수 JS |
| `yhj1024/manseryeok` | ✅ | 부분 | 미확인 | TypeScript, 음력 입력 지원 |
| `lunar-javascript` (6tail) | ❌ (중국) | ❌ | 미확인 | 한국 시주 별도 구현 필요 |

**선택: `@fullstackfamily/manseryeok`**
- 한국 표준 자오선(135도) 기준 진태양시 보정 내장
- `calculateSaju()` 하나로 년주·월주·일주·시주 + 한자 + 오행 전부
- 24절기 사주월 매핑, 60갑자 오행 수정 이력 투명

**검증 방식 (Eugene 확정)**: Playwright로 무작위 10건 생성 → 포스텔러(pro.forceteller.com) 스크래핑 결과와 4글자 비교 → 10/10 일치 시 Go. 불일치 시 [§9 트리거 0].

### 리스크 2: LLM 프롬프트 품질

A-2 Kill 이유 1에서 제기됨. §6에서 초안 B(친근한 TV 역술가) 확정으로 해결. 10명 테스트에서 "일반론적 해석" 피드백 나오면 프롬프트 튜닝.

### 리스크 3: 50% 랜덤 4지선다 UX

A-2 Kill 이유 3에서 제기됨. Eugene 확정대로 50% 유지. 10명 테스트 후 피드백 따라 B(100%) 또는 C(제거)로 조정.

---

## §4. 프로젝트 구조 스켈레톤

### 폴더 레이아웃

```
sajutalk/
├── app/
│   ├── api/
│   │   ├── session/route.ts           익명 세션 생성·조회
│   │   ├── manse/route.ts             만세력 계산 엔드포인트
│   │   ├── classify/route.ts          고민 영역 LLM 분류 (S3)
│   │   ├── interpret/route.ts         긴 해석 스트리밍 (S6)
│   │   ├── qna/route.ts               Q&A 답변 스트리밍 (S9)
│   │   └── summary/route.ts           정리 응답 스트리밍 (S11)
│   ├── (screens)/
│   │   ├── page.tsx                   화면 1 (랜딩·생시 입력)
│   │   ├── concern/page.tsx           화면 2 (고민 입력)
│   │   ├── pattern/page.tsx           화면 3 (반복 패턴 4지선다)
│   │   └── chat/page.tsx              화면 4·5 (대화 + 조건부 배너)
│   └── layout.tsx
├── lib/
│   ├── manse/
│   │   ├── engine.ts                  @fullstackfamily/manseryeok 래퍼
│   │   └── verify.spec.ts             Playwright 10건 검증 스크립트
│   ├── prompts/
│   │   ├── interpret.ts               §6-a 긴 해석 system prompt
│   │   ├── qna.ts                     §6-b Q&A 답변 system prompt
│   │   ├── summary.ts                 §6-c 정리 응답 system prompt
│   │   └── classify.ts                §6-d S3 영역 분류 prompt
│   ├── state/
│   │   └── chat-machine.ts            화면 4 상태 머신 (A~F + 50% 로직)
│   ├── supabase/
│   │   ├── client.ts                  클라이언트 측 Supabase 연결
│   │   └── server.ts                  서버 측 Supabase (RLS 우회용)
│   └── session/
│       └── anonymous.ts               localStorage·쿠키 기반 세션 ID
├── components/
│   ├── ui/                            shadcn/ui 컴포넌트
│   └── chat/
│       ├── MessageBubble.tsx
│       ├── StreamingMessage.tsx
│       └── FourChoiceInline.tsx       답변 말미 삽입용 4지선다
├── tests/
│   └── scenarios.spec.ts              A-1 시나리오 1·2 E2E
├── .env.local
├── supabase/
│   └── migrations/
│       └── 001_initial.sql            초기 스키마
└── CLAUDE.md                          Claude Code 컨텍스트 파일
```

### DB 스키마 개요

```
sessions (익명 세션 단위)
  - id (uuid, primary key)
  - anonymous_id (text, unique) ← localStorage에서 전달
  - name (text)
  - gender (text)
  - birth_date (date)
  - birth_time (time, nullable)
  - time_unknown (boolean, default false)
  - manse_data (jsonb) ← 만세력 결과 캐시
  - parent_info (jsonb, nullable)
  - environment_info (jsonb, nullable)
  - postpone_flag (boolean, default false)
  - session_count (int, default 0)
  - created_at, updated_at

conversations (세션별 대화 단위)
  - id (uuid, primary key)
  - session_id (uuid, FK)
  - conversation_seq (int)
  - concern_text (text)
  - concern_category (text)
  - pattern_answer (text)
  - long_interpretation (text)
  - question_count_used (int, default 0)
  - inline_choice_question (text, nullable)
  - inline_choice_answer (text, nullable)
  - summary_text (text, nullable)
  - completed_at (timestamp, nullable)
  - created_at

qna_turns (Q&A 턴별)
  - id
  - conversation_id (uuid, FK)
  - turn_number (int)
  - user_question (text)
  - ai_answer (text)
  - created_at
```

정확한 SQL migration과 RLS는 Claude Code가 `supabase db push`로 생성. `anonymous_id` 저장된 uuid로 익명 세션 식별.

---

## §5. 빌드 순서

Claude Code가 이 순서대로 실행. 각 단계 끝에 한 줄 진행 보고.

```
1. `npm create next-app sajutalk` — TypeScript·Tailwind·App Router·src dir 선택
2. `npx shadcn init` — slate color, CSS variables 사용
3. 필요 shadcn 컴포넌트 설치: button, input, card, radio-group, checkbox, select
4. .env.local 생성 + Anthropic·Supabase 환경변수 채우기
5. Supabase 연결 (lib/supabase/client.ts, lib/supabase/server.ts)
6. DB 스키마 migration 생성·적용 (`supabase db push`)
7. 만세력 엔진 설치·래핑 (lib/manse/engine.ts)
   → `npm i @fullstackfamily/manseryeok`
   → calculateSaju 래퍼: 입력(이름·생년월일·시간·time_unknown) →
     출력(년주·월주·일주·시주 + 한자 + 오행 요약)
8. [검증 0 선행] Playwright 설치·실행
   → `npm i -D @playwright/test && npx playwright install chromium`
   → lib/manse/verify.spec.ts 작성:
     - 무작위 생년월일시 10건 생성 (1950~2005년, 0~23시 포함)
     - 각 건: (a) @fullstackfamily/manseryeok 결과 생성
       (b) pro.forceteller.com Playwright 접속 → 동일 입력 → 결과 스크래핑
     - 년주·월주·일주·시주 4글자 비교
     - 10/10 일치 시 통과, 1건이라도 불일치 시 STOP + 불일치 상세 출력
   → 실행: `npx playwright test lib/manse/verify.spec.ts`
   → [§9 트리거 0] 불일치 시 Eugene 호출
9. lib/prompts/ 에 §6-a·§6-b·§6-c·§6-d prompts 저장
10. lib/state/chat-machine.ts 작성 — 화면 4 상태 머신 A~F 구현
    (xstate 또는 useReducer. Claude Code 재량)
11. API routes 스텁: session → manse → classify → interpret → qna → summary
12. 화면 1 (app/page.tsx) — 생시 입력 폼 + 만세력 호출 + 세션 저장
13. 화면 2 (app/concern/page.tsx) — 4지선다 카드 + 직접 쓰기 확장
14. 화면 3 (app/pattern/page.tsx) — 영역별 반복 패턴 4지선다
    + LLM 자동 분류, 기타는 일반 패턴 fallback
15. 화면 4 (app/chat/page.tsx) — 상태 머신 적용 + 긴 해석·Q&A 스트리밍
    + 50% 랜덤 4지선다 삽입 + [이제 됐어요] + 내부 카운터
16. 화면 5 확장 (화면 4 안에서 조건부 렌더링) — 3지선다 + fallback 폼
17. A-1 시나리오 1·2 수동 E2E 확인 (localhost)
18. Vercel 배포 + 환경변수 설정
19. [검증 1·2] §10 시나리오 수동 E2E 재확인 (production URL)
20. §12 완료 보고 생성
```

---

## §8. Claude Code 재량 범위

혼자 결정 OK (물어보지 말고 진행):

1. shadcn/ui 컴포넌트는 필요한 것만 `npx shadcn add <name>`로 추가. 목록 질문 없이 진행
2. Tailwind 클래스 세부는 A-2 §4-a ASCII 와이어프레임 요소 배치만 보존하면 자유
3. 파일·폴더 이름 세부 (§4 구조 안에서)
4. 에러 UI 문구는 "잠시 후 다시 시도해주세요" 기본형
5. git commit 메시지 (시크릿 포함 금지)
6. 의존성 버전: major는 §2 고정, minor·patch 자유
7. 주석 스타일
8. 단위 테스트 작성 여부 (E2E는 §10, 단위는 재량)
9. 기본 접근성 (alt·aria-label·focus ring)
10. 로딩 인디케이터·애니메이션·트랜지션 디자인 — A-2 §4-b UX 의도 내에서
11. 상태 머신 구현 라이브러리 선택 (xstate vs useReducer vs Zustand)
12. 만세력 결과 jsonb 구조 세부 — 프롬프트에 넣을 포맷 결정

---

## §9. bypass 멈춤 트리거

반드시 사람에게 물어야 하는 경우. bypass 모드여도 정지.

### [트리거 0] 만세력 검증 실패 (사주톡 특화)

Playwright 검증 10/10 미만일 때:

```
🛑 [트리거 0] 만세력 검증 실패
상황: 10건 중 N건 불일치
불일치 상세:
  - 건 1: 1990-05-15 14:30 M
    @fullstackfamily 결과: 경오 신사 경진 계미
    포스텔러 결과:       경오 신사 경진 계미  ← 일치
  - 건 3: 1985-02-03 23:45 F
    @fullstackfamily 결과: 갑자 정축 무인 임자
    포스텔러 결과:       갑자 병자 무인 임자  ← 불일치 (월주)
  [모든 불일치 건 나열]

추정 원인: [Claude Code 추정]
제안: (1) 라이브러리 v1.0.8+ 업데이트 확인
     (2) yhj1024/manseryeok 후보 2로 전환
     (3) 포스텔러 스크래핑 정확도 재확인
     (4) Eugene 판단
```

### [트리거 1] 시크릿 커밋 시도

ANTHROPIC_API_KEY·SUPABASE_SERVICE_ROLE_KEY가 .env.local 밖 파일이나 git에 포함되려 할 때 정지. .env.local은 gitignore 확인 후 진행.

### [트리거 2] 데이터·파일 영구 삭제

`rm -rf`, `DROP TABLE`, 파일 다수 삭제 직전 정지. 1~2개 파일 교체는 재량(§8).

### [트리거 3] 외부 결제·유료 티어 승격

- Anthropic 크레딧 $20 초과 시 정지
- Supabase·Vercel 유료 티어 업그레이드 요청 발생 시 정지

### [트리거 4] 첫 공개 배포 직전

Vercel production 배포 직전 정지 (preview는 트리거 아님).

### [트리거 5] 민감 개인정보 저장 설계 이슈 (사주톡 특화)

- DB 컬럼에 "이혼"·"조실부모" 같은 원 단어가 들어가려 할 때 정지
- 로그·에러 메시지에 사용자 개인 생시·이름 노출되려 할 때 정지

**메시지 포맷 (모든 트리거 공통)**:
```
🛑 [트리거 N] 발동
상황: [구체적으로 지금 뭘 하려던 중]
제안: [Eugene에게 묻는 선택지]
계속 진행 / 중단 / 다른 방법
```

---

## §9.5. 재시도 상한

### [상한 1] 같은 에러 3회 반복

같은 스택 트레이스나 메시지가 3번 재현되면 정지.

### [상한 2] 한 단계 30분 초과

§5 빌드 순서 한 항목이 30분 넘어가면 정지.

### [상한 3] 총 세션 2시간 초과

완료 안 됐어도 2시간 지나면 §12 완료 보고 작성 후 정지.

**메시지 포맷**:
```
⏸ [상한 N] 도달
시도한 것: [3가지]
실패 양상: [공통 증상]
추측 원인: [Claude Code 추정]
Eugene 의견 필요
```

---

## §10. 검증 방법

### [검증 0] 만세력 10건 일치 (자동, Playwright)

§5 빌드 순서 8번에 포함. 10/10 일치 시 PASS. 불일치 시 [§9 트리거 0].

### [검증 1] A-1 시나리오 1 (이직 고민 여성, 첫 세션, 3번 질문 다 씀)

**실행**: production URL에 Eugene이 직접 접속, 수동 따라 확인

**PASS 조건** (모두 충족):
- 화면 1에서 이름·성별·생년월일·시간 입력 가능, "시간을 몰라요" 체크박스 동작
- 화면 2에서 "이직·일" 카드 탭 시 즉시 화면 3 전환 (LLM 분류 스킵)
- 화면 3에서 이직 영역 맞춤 4지선다 표시, 탭 시 화면 4 전환
- 화면 4 상태 A 배너 1~2초 후 상태 B 해석 스트리밍 자동 시작
- 긴 해석 5문장 이상 스트리밍, 이름 부르기 포함 ("○○님, ...")
- 해석 완료 후 입력창 enabled, "혹시 더 궁금한 거 있으세요?" 프롬프트
- 질문 1~3 전송 가능, 각 답변 스트리밍, 원 해석과 모순 없음
- 3번 다 씀 후 자동 상태 F 전환, "세 번 물어봐주셨으니..." 전환 문구 포함
- 정리 응답 아래 화면 5 배너 자동 노출 ([부모님][환경][다음에요])
- "부모님 정보" 탭 시 fallback 라디오 4개 확장

### [검증 2] A-1 시나리오 2 (연애 고민 여성, 2회차 세션, 2번만 씀)

**실행**: [검증 1] 완료 후 같은 브라우저에서 이어서 수동

**PASS 조건**:
- 화면 1 스킵, 화면 2 직접 진입 (저장된 생시 사용 확인)
- "연애" 카드 탭 시 화면 3 연애 영역 질문 표시
- 화면 4 해석에 "지난번 ○○ 고민 때 말씀하신 거랑 연결해서..." 포함
- 질문 1 답변 → 질문 2 답변 스트리밍 → (50% 확률) 답변 말미에 4지선다 표시
- 4지선다 뜨면 입력창 disabled, 탭 후 enabled
- 4지선다 안 뜨면 답변 완료 즉시 입력창 enabled
- [이제 됐어요] 탭 시 즉시 정리 응답, 전환 문구 없음
- 정리 응답에 4지선다 답 내용 반영 확인 (탭했을 경우)
- 화면 5 배너 표시 안 됨 확인 (3번 미만)

**검증 노트**: 50% 랜덤은 Eugene이 2회차 세션 5번 돌려서 50% 근처인지만 확인.

---

## §13. Kill / Go / Hold 판정

**Go — Claude Code bypass 세션 진입**

근거 3줄:
1. PART 1 Eugene 직접 읽고 행동할 내용(체크리스트·프롬프트·결정 항목·복붙 템플릿·완료 보고 포맷) 모두 명확. Eugene 혼자 결정할 게 더 없음
2. PART 2 Claude Code 참조 내용(스택·리스크·구조·빌드 순서·재량·트리거·상한·검증) 완성. [SO 결정 필요] 7개 모두 해결. 미해결 이슈 없음
3. 만세력 Playwright 검증이 자동화된 첫 안전장치라 bypass 모드 위험 줄임. 10/10 일치 확인 후에만 UI 작업 진행

---

## Claude의 적대적 압박 — 이 빌드 지침서를 죽일 이유 세 가지

**1. 만세력 Playwright 검증이 포스텔러 UI 변경에 취약**

포스텔러가 프론트엔드 업데이트하면 Playwright 셀렉터 무효화. 그러면 [검증 0] 검증 실패가 아니라 "스크래핑 실패"로 오작동. Claude Code가 "라이브러리 불일치" vs "스크래핑 오류"를 구분 못 하고 [트리거 0] 오발동. 백업으로 2~3개 고정 생시(Eugene 본인·아는 사람)의 정답 값을 하드코딩해 이중 확인하는 게 더 안전.

**2. 상태 머신 복잡도가 Claude Code 2시간 상한 초과 가능**

화면 4의 6상태 × 2회차 분기 × 50% 랜덤 조건은 xstate·useReducer 어느 걸 고르든 구현·디버깅에 긴 시간 필요. §5 빌드 순서 10·15번이 합쳐서 1시간 넘어갈 가능성 높음. 그러면 [상한 3] 2시간 걸리고 미완료 상태로 정지. 첫 세션은 상태 머신 + 화면 4 스트리밍까지만 목표로 줄이고, 화면 5 조건부 요청은 두 번째 세션으로 쪼개는 게 현실적.

**3. §6 system prompt의 "역술가 톤"이 실제 해석 품질을 보장하지 않음**

A-2 Kill 이유 1에서 이미 제기된 우려가 해결 안 됨. §6-a 초안 B가 아무리 잘 쓰여도 Claude가 생성하는 실제 해석이 "내 얘기네" 체감을 만드는지는 LLM 출력을 직접 봐야 확인. 10명 테스트 전에 Eugene이 본인 만세력으로 해석 1~2회 돌려서 "이 톤 OK인가" 확인하는 단계가 §5 빌드 순서에 빠져 있음. 17번 수동 E2E에 포함하거나 별도 프롬프트 검증 단계 추가 고려.
