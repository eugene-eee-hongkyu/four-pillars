# SDK 52 web 빌드는 expo CLI가 require('expo-asset') 등 transitive deps를 직접 호출.
# pnpm default strict resolution이라 매번 명시 dependency 추가해야 하는 burden.
# shamefully-hoist=true → 모든 transitive deps를 root node_modules에 hoist (npm 동작).
# Vercel pnpm v10·local pnpm v11 모두 적용.
shamefully-hoist=true
